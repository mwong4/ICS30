#include <iostream>

using namespace std;

int nChooseR (int, int, int, int)

int main()
{
    int nValue, rValue;
    int result = 1;
    int nCounter, rCounter = 0;

    cin >> nValue >> rValue;

    nChooseR(nValue, rValue, result, nCounter, rCounter)

}

int nChooseR(int nValue, int rValue, int result, int nCounter, int rCounter)
{
    if(nCounter == rCounter)
    {
        nCounter ++;
    }


}
